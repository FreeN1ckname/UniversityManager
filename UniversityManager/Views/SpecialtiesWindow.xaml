<Window x:Class="UniversityManager.Views.SpecialtiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityManager.Views"
        mc:Ignorable="d"
        Title="Специальности" Height="450" Width="800" 
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <DataTemplate x:Key="listTemplate">
            <StackPanel Orientation="Vertical" Margin="10,0,0,0">
                <TextBlock>
                        <Bold>Специальность: </Bold> 
                        <TextBlock Text="{Binding Name}"/>
                    </TextBlock>
                <TextBlock>
                        <Bold>Код: </Bold> 
                        <TextBlock Text="{Binding Code}"/>
                    </TextBlock>
                <TextBlock>
                        <Bold>Информация: </Bold> 
                        <TextBlock Text="{Binding Info}"/>                       
                    </TextBlock>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ListBox x:Name="listSpecialties" ItemTemplate="{StaticResource listTemplate}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Window>
